# Betinho Sistema - Environment Variables

# REQUIRED: JWT Secret for token signing
# Generate a strong random secret for production
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# OPTIONAL: Fixed salt for default owner account
# Generate a custom salt for better security
# Example: openssl rand -hex 32
# If not set, uses a default fixed salt
OWNER_SALT=your-custom-owner-salt-change-this-for-production

# Optional: Vercel KV for persistent storage (recommended for production)
# KV_REST_API_URL=your-vercel-kv-rest-api-url
# KV_REST_API_TOKEN=your-vercel-kv-rest-api-token
# KV_REST_API_READ_ONLY_TOKEN=your-vercel-kv-read-only-token

# Optional: Session configuration
# SESSION_EXPIRY_HOURS=24

# Environment (automatically set by Vercel)
# NODE_ENV=production  # Disables debug endpoints in production
